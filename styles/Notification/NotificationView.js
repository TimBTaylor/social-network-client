import { StyleSheet } from "react-native";

export const NotificationViewStyles = StyleSheet.create({
  container: {
    position: "absolute",
    zIndex: 1,
    height: "100%",
    width: "100%",
  },
});
